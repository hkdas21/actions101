name: First Workflow

on: [push]

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "Fist GitHub Action Job"

        - name: Checkout
          uses: actions/checkout@v6

        - name: List files
          run: ls

        - name: Read file
          run: cat README.md
            