name: First Workflow

on: [push]

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6

        - name: Welcome message
          run: |
            echo "Fist GitHub Action Job"
            ls -ltra
            cat README.md
            
      #  - name: List files
      #   run: ls
      #
      # - name: Read file
      #   run: cat README.md