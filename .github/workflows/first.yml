name: First Workflow

on: workflow_dispatch

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v6

        - name: Multiple Commands
          run: |
            echo "Fist GitHub Action Job"
            ls -ltra
            cat README.md

        - name: ASCII
          run: cowsay -f dog "WOF WOF" >> dog.txt
            
      #  - name: List files
      #   run: ls
      #
      # - name: Read file
      #   run: cat README.md